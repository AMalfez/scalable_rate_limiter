config:
  target: "http://localhost:8000"
  phases:
    - duration: 10
      arrivalRate: 100

scenarios:
  - flow:
      - post:
          url: "/api/order"
          json:
            userId: 1
            productId: 123